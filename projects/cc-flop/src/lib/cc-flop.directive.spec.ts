import { CcFlopDirective } from './cc-flop.directive';

describe('CcFlopDirective', () => {
  it('should create an instance', () => {
    const directive = new CcFlopDirective();
    expect(directive).toBeTruthy();
  });
});
